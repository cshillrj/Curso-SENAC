@model List<itemPedido>

    <h2>Listagem de Pedidos</h2>
    <table border="2">
        <thead>
            <tr>
                <th>Descrição</th>
            </tr>
        </thead>
        <tbody>
            @foreach(itemPedido item in Model)
            {
                <tr>
                    <td>@item.descricao</td>
                </tr>
            }
        </tbody>
    </table>

    <p>Total dos Pedidos é: @pedido.Total()</p>